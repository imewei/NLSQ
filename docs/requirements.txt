# Documentation dependencies for local development
#
# Sphinx and friends live in pyproject.toml's [dev] extra, not core dependencies.
#
# For local documentation builds:
#   pip install -e ".[dev]"
#
# Or use this requirements file:
#   pip install -r docs/requirements.txt

-e .[dev]
